<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/holo_blue_dark">

    <TextView
        android:id="@+id/title"
        android:layout_width="500dp"
        android:layout_height="wrap_content"
        android:text="Welcome to the American Museum of Natural History"
        android:textSize="30dp"
        android:textAlignment="center"
        android:layout_centerHorizontal="true"
        android:textColor="@android:color/holo_green_light"
        android:fontFamily="monospace"
        android:textStyle="normal|bold"
        android:textAllCaps="true" />

    <TextView
        android:id="@+id/main_paragraph"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="The Museum is open daily from 10 am to 5:45 pm except on Thanksgiving and Christmas. The museum has several exhibition that you can explore, enjoy and learn!"
        android:textSize="18dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/title"
        android:padding="30dp"
        android:textColor="@android:color/background_light" />
    <Button
        android:id="@+id/button_start"
        android:textSize="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/main_paragraph" />


</RelativeLayout>
